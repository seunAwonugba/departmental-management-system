import org.junit.Test

import org.junit.Assert.*

class NonAcademicStaffTest {

    /**
     * Test carried out to check if the nonAcademicFunction() does exactly what is expected
     */

    @Test
    fun nonAcademicStaffFunction() {
        assertEquals("I'm a non-academic staff", "I'm a non-academic staff")
    }
}