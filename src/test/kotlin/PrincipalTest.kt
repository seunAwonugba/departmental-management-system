import org.junit.Test

import org.junit.Assert.*

class PrincipalTest {

    /**
     * Test carried out to check if the functionPrincipal() does exactly what is expected
     */

    @Test
    fun functionPrincipal() {
        assertEquals("I expel students", "I expel students")
    }
}